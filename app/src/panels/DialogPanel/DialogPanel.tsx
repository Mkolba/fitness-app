import React from "react";
import {Panel, PanelProps} from "@vkontakte/vkui";
import './style.scss';

export const DialogPanel: React.FC<PanelProps> = ({
 nav
}) => {
  return (
    <Panel nav={nav}>

    </Panel>
  )
}